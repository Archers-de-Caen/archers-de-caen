{% macro thumbnail(link, img, title) %}
    <a class="thumbnail" href="{{ link }}" title="{{ title }}">
        <img src="{{ img }}" alt="Photo de la galerie">
        <p>{{ title }}</p>
    </a>
{% endmacro %}
