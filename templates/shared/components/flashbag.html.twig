{% if flashbag %}
    <div class="mb-3">
        {% for type, flashes in flashbag %}
            {% for flash in flashes %}
                <div class="alert alert-{{ type }}">
                    {{ flash }}
                </div>
            {% endfor %}
        {% endfor %}
    </div>
{% endif %}
