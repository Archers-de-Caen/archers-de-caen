{% block ckeditor_row %}
    <style>
        .ck-editor {
            max-width: 700px;

            margin-bottom: 30px!important;
        }

        .ck-editor__editable_inline {
            min-height: 500px;
        }

        .ck-content p, .ck-content h1, .ck-content h2, .ck-content h3, .ck-content h4 {
            color: black;
        }
    </style>

    <label for="editor">Contenu</label>

    <textarea id="editor" name="{{ form.vars.full_name }}">
        {{ form.vars.data }}
    </textarea>
{% endblock %}
