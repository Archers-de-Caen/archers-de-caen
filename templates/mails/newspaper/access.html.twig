{# @var string password #}

{% extends layout %}

{% block body %}
    <table width="100%" cellpadding="0" cellspacing="0" role="presentation" style="margin-top: 1rem">
        <tr>
            <td align="center">
                <a href="{{ absolute_url(path(constant('App\\Http\\Landing\\Controller\\Page\\NewspaperController::ROUTE'), { password: password })) }}"
                   class="button button-primary"
                >
                    Accéder aux gazettes
                </a>
            </td>
        </tr>
    </table>
{% endblock %}
